# Copy to .env for local development (never commit .env)

# Backend
PORT=3001
CLIENT_URL=http://localhost:5173
MONGODB_URI=mongodb://127.0.0.1:27017/draw-and-guess

# Frontend — only needed when the API runs on a different host (e.g. split deploy)
VITE_SOCKET_URL=http://localhost:3001
